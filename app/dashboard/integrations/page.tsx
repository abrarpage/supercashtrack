import { IntegrationsPageContent } from "./integrations-page-content";

export const metadata = { title: "Integrasi — Cash Tracker" };

export default async function IntegrationsPage() {
  return <IntegrationsPageContent />;
}
