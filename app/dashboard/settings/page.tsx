  
import { SettingsPageContent } from "./settings-page-content";

export const metadata = { title: "Pengaturan — Cash Tracker" };

export default async function SettingsPage() {
   
  return <SettingsPageContent />;
}
