import { CategoriesPageContent } from "./categories-page-content";

export const metadata = { title: "Kategori — Cash Tracker" };

export default async function CategoriesPage() {
  return <CategoriesPageContent />;
}
