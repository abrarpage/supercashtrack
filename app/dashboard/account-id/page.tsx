  
import { AccountIdPageContent } from "./account-id-page-content";

export const metadata = { title: "Account ID — Cash Tracker" };

export default async function AccountIdPage() {
   
  return <AccountIdPageContent />;
}
